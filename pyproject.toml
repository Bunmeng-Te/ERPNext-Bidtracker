[project]
name = "bid_tracker"
version = "0.1.0"
description = "ERPNext Bid Tracker custom app"
requires-python = ">=3.10"
authors = [{name = "Rudra Pandey"}]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
